{
    "name": "tailwind",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build-css": "npx tailwindcss build -i src/styles.css -o public/styles.css -m",
        "build": "NODE_ENV=production npx tailwindcss build -i src/styles.css -o public/styles.css -m"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {},
    "devDependencies": {
        "autoprefixer": "^10.2.6",
        "postcss": "^8.3.5",
        "tailwindcss": "^2.2.4"
    }
}